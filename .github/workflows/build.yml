name: Build Action
on:
  push:
    branches:
      - android
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: Build the app
        run: ./gradlew build
